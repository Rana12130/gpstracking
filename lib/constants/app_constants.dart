// lib/constants/app_constants.dart
class AppConstants {
  static const double radiusM = 60.0;
  static const double exitRadiusM = 70.0;
  static const String chimeAsset = 'assets/chime.mp3';
  static const Duration listenDuration = Duration(seconds: 5);
  static const Duration pauseDuration = Duration(seconds: 2);
  static const Duration repeatDelay = Duration(seconds: 5);
  static const Duration locationTimeout = Duration(seconds: 10);
}